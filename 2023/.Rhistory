library(tidyverse)
input <- read_tsv("https://adventofcode.com/2023/day/1/input") |> pull()
getwd()
input <- read_tsv("01.input.txt") |> pull()
input <- read_tsv("01.input.txt", col_names = FALSE) |> pull()
input <- read_tsv("01.input.txt", col_names = FALSE)
input
input |> mutate(
n = case_when(
length(str_split_i(X1)) == 1 ~ str_split_i(X1)[1],
length(str_split_i(X1)) > 1 ~ as.numeric( str_c( str_split_i(X1)[1], str_split_i(X1)[length(str_split_i(X1))] ) )
)
)
parse_number(input$X1)
input |> mutate(
n1 = as.character(parse_number(X1)),
n2 = case_when(
length(str_split_i(n2)) == 1 ~ str_split_i(n2)[1],
length(str_split_i(n2)) > 1 ~ as.numeric( str_c( str_split_i(n2)[1], str_split_i(n2)[length(str_split_i(n2))] ) )
)
)
input |> mutate(
n1 = as.character(parse_number(X1)),
#n2 = case_when(
#  length(str_split_i(n2)) == 1 ~ str_split_i(n2)[1],
#  length(str_split_i(n2)) > 1 ~ as.numeric( str_c( str_split_i(n2)[1], str_split_i(n2)[length(str_split_i(n2))] ) )
#)
)
input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
#n2 = case_when(
#  length(str_split_i(n2)) == 1 ~ str_split_i(n2)[1],
#  length(str_split_i(n2)) > 1 ~ as.numeric( str_c( str_split_i(n2)[1], str_split_i(n2)[length(str_split_i(n2))] ) )
#)
)
input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(str_split_i(n2)) == 1 ~ str_split_i(n2)[1],
nchar(str_split_i(n2)) > 1 ~ as.numeric( str_c( str_split_i(n2)[1], str_split_i(n2)[length(str_split_i(n2))] ) )
)
)
input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(str_split_i(n1)) == 1 ~ str_split_i(n1)[1],
nchar(str_split_i(n1)) > 1 ~ as.numeric( str_c( str_split_i(n1)[1], str_split_i(n1)[length(str_split_i(n1))] ) )
)
)
input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = length(str_split_i(n1))
#n2 = case_when(
#  length(str_split_i(n1)) == 1 ~ str_split_i(n1)[1],
#  length(str_split_i(n1)) > 1 ~ as.numeric( str_c( str_split_i(n1)[1], str_split_i(n1)[length(str_split_i(n1))] ) )
#)
)
input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = if_else(!is.na(n1), length(str_split_i(n1)), NA)
#n2 = case_when(
#  length(str_split_i(n1)) == 1 ~ str_split_i(n1)[1],
#  length(str_split_i(n1)) > 1 ~ as.numeric( str_c( str_split_i(n1)[1], str_split_i(n1)[length(str_split_i(n1))] ) )
#)
)
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = if_else(!is.na(n1), length(str_split_i(n1)), NA)
#n2 = case_when(
#  length(str_split_i(n1)) == 1 ~ str_split_i(n1)[1],
#  length(str_split_i(n1)) > 1 ~ as.numeric( str_c( str_split_i(n1)[1], str_split_i(n1)[length(str_split_i(n1))] ) )
#)
)
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
#n2 = if_else(!is.na(n1), length(str_split_i(n1)), NA)
#n2 = case_when(
#  length(str_split_i(n1)) == 1 ~ str_split_i(n1)[1],
#  length(str_split_i(n1)) > 1 ~ as.numeric( str_c( str_split_i(n1)[1], str_split_i(n1)[length(str_split_i(n1))] ) )
#)
)
input
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ as.numeric( str_c( str_sub(n1, 1), str_sub(n1, -1) ) )
)
)
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
#nchar(n1) > 1 ~ as.numeric( str_c( str_sub(n1, 1), str_sub(n1, -1) ) )
)
)
input
input = input |> mutate(
n1 = as.numeric(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
#nchar(n1) > 1 ~ str_c( str_sub(n1, 1), str_sub(n1, -1) )
)
)
input
input = input |> mutate(
n1 = as.numeric(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1), str_sub(n1, -1) )
)
)
input = input |> mutate(
n1 = as.numeric(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( as.character(str_sub(n1, 1)), as.character(str_sub(n1, -1)) )
)
)
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( as.cstr_sub(n1, 1), str_sub(n1, -1) )
)
)
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1), str_sub(n1, -1) )
)
)
input
input = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1), str_sub(n1, -1) )
)
)
input
input <- read_tsv("01.input.txt", col_names = FALSE)
output = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1), str_sub(n1, -1) )
)
)
output
output = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )
)
)
output
output = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ as.numeric( str_c( str_sub(n1, 1, 1), str_sub(n1, -1) ) )
)
)
output = input |> mutate(
n1 = as.character(gsub("\\D", "", X1)),
n2 = as.numeric( case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )
) )
)
output
output = input |> mutate(
n1 = as.numeric(gsub("\\D", "", X1)),
n2 = case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )
)
)
output = input |> mutate(
n1 = gsub("\\D", "", X1),
n2 = as.numeric( case_when(
nchar(n1) == 1 ~ n1,
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )
) )
)
output
sum(output$n2)
input <- read_tsv("01.example.txt", col_names = FALSE)
output = input |> mutate(
n1 = gsub("\\D", "", X1),   # strip non-integer characters
n2 = as.numeric( case_when( # convert result to numeric
nchar(n1) == 1 ~ n1,      # if only 1 number, use this
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )  # if multiple numbers, paste first and last together
) )
)
sum(output$n2)
output = input |> mutate(
n1 = gsub("\\D", "", X1),   # strip non-integer characters
n2 = as.numeric( case_when( # convert result to numeric
nchar(n1) == 1 ~ str_c( n1, n1 ),      # if only 1 number, use this twice
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )  # if multiple numbers, paste first and last together
) )
)
sum(output$n2)
input <- read_tsv("01.input.txt", col_names = FALSE)
output = input |> mutate(
n1 = gsub("\\D", "", X1),   # strip non-integer characters
n2 = as.numeric( case_when( # convert result to numeric
nchar(n1) == 1 ~ str_c( n1, n1 ),      # if only 1 number, use this twice
nchar(n1) > 1 ~ str_c( str_sub(n1, 1, 1), str_sub(n1, -1) )  # if multiple numbers, paste first and last together
) )
)
sum(output$n2)
